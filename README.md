# RoboticsII_Final_Project

## Setting up communication between robots
1. Ensure robots are connected to the same network
2. om both robots, run ```export ROS_DOMAIN_ID=X```, where X is replaced by a number 1-232. It must be the same number on both robots.
3. Run the docker setup
